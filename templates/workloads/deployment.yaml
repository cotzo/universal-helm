{{- include "chartpack.validate" . -}}
{{- if eq .Values.workloadType "Deployment" }}
apiVersion: apps/v1
kind: Deployment
{{ include "chartpack.metadata" (dict "context" $ "workload" true) }}
spec:
  {{- if not (or .Values.autoscaling.hpa.enabled .Values.autoscaling.keda.enabled) }}
  replicas: {{ .Values.workloads.deployment.replicaCount }}
  {{- end }}
  {{- with .Values.workloads.deployment.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.workloads.deployment.minReadySeconds }}
  minReadySeconds: {{ . }}
  {{- end }}
  {{- with .Values.workloads.deployment.revisionHistoryLimit }}
  revisionHistoryLimit: {{ . }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "chartpack.selectorLabels" . | nindent 6 }}
  template:
    {{- include "chartpack.podTemplate" . | nindent 4 }}
{{- end }}
