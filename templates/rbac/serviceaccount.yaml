{{- if .Values.rbac.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
{{ include "chartpack.metadata" (dict "name" (include "chartpack.rbac.serviceAccountName" .) "context" $ "extraAnnotations" .Values.rbac.serviceAccount.annotations "extraLabels" .Values.rbac.serviceAccount.labels) }}
{{- if not (kindIs "invalid" .Values.rbac.serviceAccount.automountServiceAccountToken) }}
automountServiceAccountToken: {{ .Values.rbac.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- with .Values.rbac.serviceAccount.imagePullSecrets }}
imagePullSecrets:
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end }}
